using System.ComponentModel;
using ColorMod.Registry;

namespace FFTColorMod.Configuration
{
    [StoryCharacter(SpriteNames = new[] { "dami" }, DefaultTheme = "original")]
    public enum DycedargColorScheme
    {
        [Description("Original")]
        original
    }
}