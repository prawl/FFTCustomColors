using System.ComponentModel;
using ColorMod.Registry;

namespace FFTColorMod.Configuration
{
    [StoryCharacter(SpriteNames = new[] { "zarumou" }, DefaultTheme = "original")]
    public enum ZalmoColorScheme
    {
        [Description("Original")]
        original
    }
}