using System.ComponentModel;
using ColorMod.Registry;

namespace FFTColorMod.Configuration
{
    [StoryCharacter(SpriteNames = new[] { "voru" }, DefaultTheme = "original")]
    public enum VormavColorScheme
    {
        [Description("Original")]
        original
    }
}