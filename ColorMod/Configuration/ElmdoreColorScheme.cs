using System.ComponentModel;
using ColorMod.Registry;

namespace FFTColorMod.Configuration
{
    [StoryCharacter(SpriteNames = new[] { "eru" }, DefaultTheme = "original")]
    public enum ElmdoreColorScheme
    {
        [Description("Original")]
        original
    }
}